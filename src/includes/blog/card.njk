{# Macro: PostCard(post) #}
{% macro PostCard(post) %}
<article class="post-card js-post" data-tags="{{ (post.data.tags or []).join(',') }}" data-cat="{{ post.data.category or '' }}">
  <a class="post-link" href="{{ post.url | url }}">
    <h3 class="post-title">{{ post.data.title }}</h3>
  </a>
  <div class="post-meta">
    <time datetime="{{ post.date | dateToISO }}">{{ post.date | dateDisplay }}</time>
    {% if post.data.category %}<span class="dot">•</span><span class="cat">{{ post.data.category }}</span>{% endif %}
  </div>
  {% if post.data.excerpt %}<p class="post-excerpt">{{ post.data.excerpt }}</p>{% endif %}
</article>
{% endmacro %}
