---
import BaseLayout from "../../../layouts/BaseLayout.astro";
import CTAButtons from "../../../components/CTAButtons.astro";
import Section from "../../../components/Section.astro";
import { servicesAr } from "../../../data/services";
---
<BaseLayout
  title="الخدمات | تامر منصور"
  description="خدمات احترافية في السرد البصري، أنظمة الإنتاج بالذكاء الاصطناعي، والتدريب الإبداعي."
  path="/ar/services/"
  lang="ar"
  dir="rtl"
>
  <Section
    title="خدمات مصممة للتحويل والاستمرارية"
    copy="يمكنك اختيار باقة واحدة أو دمج أكثر من خدمة ضمن شراكة إبداعية متكاملة."
    className="text-right"
  >
    <div class="grid gap-4 md:grid-cols-3">
      {
        servicesAr.map((service) => (
          <article class="glass-card p-6">
            <h2 class="text-xl font-semibold text-white">{service.title}</h2>
            <p class="mt-3 text-sm text-sand/80">{service.summary}</p>
            <ul class="mt-4 space-y-2 text-sm text-sand/80">
              {service.deliverables.map((item) => <li>• {item}</li>)}
            </ul>
          </article>
        ))
      }
    </div>
  </Section>
  <Section title="احجز مكالمة استراتيجية" copy="مكالمة قصيرة كافية لتحديد النطاق والزمن والنتيجة المتوقعة." className="text-right">
    <CTAButtons bookHref="/ar/contact/" labels={{ book: "احجز مكالمة", whatsapp: "راسلني على واتساب" }} />
  </Section>
</BaseLayout>
