---
import BaseLayout from "../../../layouts/BaseLayout.astro";
import Section from "../../../components/Section.astro";
import { withBase } from "../../../utils/withBase";

const posts = [
  {
    title: "اقفز قبل أن تشعر أنك جاهز: ملاحظات فلسطيني قرر يلاحق شغفه أخيرًا",
    date: "فبراير 2026",
    excerpt: "كيف تركت المسار “الآمن”، وبنيت حياتي حول الذكاء الاصطناعي والسرد البصري، وما الذي أقوله لأي شخص واقف على الحافة.",
    href: "/ar/blog/jump-before-you-feel-ready/"
  }
];
---
<BaseLayout
  title="المدونة | تامر منصور"
  description="مقالات وأفكار حول السرد البصري، أنظمة الذكاء الاصطناعي، وتطوير المبدعين."
  path="/ar/blog/"
  lang="ar"
  dir="rtl"
>
  <Section title="المدونة" copy="ملاحظات عملية من التجربة: سرد، أنظمة، ونمو إبداعي." className="text-right">
    <div class="space-y-4">
      {
        posts.map((post) => (
          <a href={withBase(post.href)} class="glass-card block p-6 text-right transition hover:border-gold/50">
            <p class="text-xs uppercase tracking-wide text-gold">{post.date}</p>
            <h2 class="mt-2 text-xl font-semibold text-white">{post.title}</h2>
            <p class="mt-3 text-sm text-sand/80">{post.excerpt}</p>
            <p class="mt-4 text-sm font-semibold text-gold">اقرأ المقال</p>
          </a>
        ))
      }
    </div>
  </Section>
</BaseLayout>
