---
layout: page.njk
title: Blog
---

{% for item in collections.all %}
  {% if item.data.tags and item.data.tags.includes('blog') %}
    <article class="media-card">
      <h2><a href="{{ item.url }}">{{ item.data.title }}</a></h2>
      <p>{{ item.data.excerpt }}</p>
    </article>
  {% endif %}
{% endfor %}

<p>More articles coming soon.</p>