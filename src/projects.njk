---
layout: page.njk
title: Projects
---

{% for item in collections.all %}
  {% if item.data.tags and item.data.tags.includes('project') %}
  <article>
    <h2><a href="{{ item.url }}">{{ item.data.title }}</a></h2>
    <p>{{ item.data.excerpt }}</p>
  </article>
  {% endif %}
{% endfor %}
